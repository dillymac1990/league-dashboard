// Shared position palette (Sleeper-style, sampled from a real Sleeper draft
// board screenshot) — used by the Draft Board grid, Team Composition pie,
// Points by Position chart, and player cards alike. K wasn't visible in the
// sample (this league doesn't roster one), so that value is a guess. FLEX
// has no Sleeper equivalent (it's a lineup slot, not a real position), so
// it keeps a neutral gray.
export const POS_COLOR = {
  QB: "#B8507C",
  RB: "#4FAE7F",
  WR: "#4A85C4",
  TE: "#C98A45",
  K: "#B48EE0",
  DEF: "#A8706B",
  FLEX: "#8892A6",
};
